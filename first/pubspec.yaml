name: first
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # MIT
  audioplayers: 0.17.0
  sqflite: ^1.3.0
  flutter_xlider: ^3.4.0
  circular_countdown_timer: ^0.0.8
  volume_control: ^0.1.3
  noise_meter: ^2.0.0

  # Apache 2.0
  charts_flutter: ^0.9.0

  # BSD
  path: ^1.7.0
  flutter_blue: ^0.7.3

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audio/
